version: '3.4'
services:
  editor:
    image: lblod/frontend-demo-editor:0.2.4
    links:
      - identifier:backend
  admin:
    image: lblod/frontend-resource-editor:0.1.1
    environment:
      STATIC_FOLDERS_REGEX: "^/(assets|fonts)/"
    links:
      - identifier:backend
  identifier:
    image: semtech/mu-identifier:1.3.0
  dispatcher:
    image: semtech/mu-dispatcher:1.1.0
    volumes:
      - ./config/dispatcher:/config
  database:
    image: tenforce/virtuoso:1.3.0-virtuoso7.2.2
    environment:
      SPARQL_UPDATE: "true"
      DEFAULT_GRAPH: "http://mu.semte.ch/application"
    volumes:
      - ./data/db:/data
  migrations:
    image: semtech/mu-migrations-service:0.3.0
    volumes:
      - ./config/migrations:/data/migrations
    environment:
      MU_SPARQL_TIMEOUT: "600"
  resource:
    image: semtech/mu-cl-resources:1.16.0
    volumes:
      - ./config/resources:/config
  notulenimporter:
    image: lblod/notulen-importer-service:0.3.0

