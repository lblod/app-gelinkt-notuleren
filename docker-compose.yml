version: '3.4'
services:
  editor:
    image: lblod/frontend-demo-editor:0.4.0
    links:
      - identifier:backend
  admin:
    image: lblod/frontend-editor-admin:0.2.2
    environment:
      STATIC_FOLDERS_REGEX: "^/(assets|fonts)/"
    links:
      - identifier:backend
  identifier:
    image: semtech/mu-identifier:1.4.0
  dispatcher:
    image: semtech/mu-dispatcher:1.1.2
    volumes:
      - ./config/dispatcher:/config
  database:
    image: tenforce/virtuoso:1.3.2-virtuoso7.2.5.1
    environment:
      SPARQL_UPDATE: "true"
      DEFAULT_GRAPH: "http://mu.semte.ch/application"
    volumes:
      - ./data/db:/data
  migrations:
    image: semtech/mu-migrations-service:0.3.1
    volumes:
      - ./config/migrations:/data/migrations
    environment:
      MU_SPARQL_TIMEOUT: "600"
  resource:
    image: semtech/mu-cl-resources:1.17.1
    volumes:
      - ./config/resources:/config
  notulenimporter:
    image: lblod/notulen-importer-service:0.3.0

